namespace Vantage.Domain.Tests;

/// <summary>
/// Proves the test harness (project references, xUnit runner) works. Real
/// domain tests now live alongside this one under Metrics/ — this file
/// itself couldn't be removed due to an environment quirk during scaffolding,
/// so it stays as a harmless, permanently-green sanity check.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void TestHarness_IsWiredUp()
    {
        Assert.True(true);
    }
}
