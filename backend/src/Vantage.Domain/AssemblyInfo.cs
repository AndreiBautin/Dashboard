// This assembly holds entities, value objects, and evaluation strategies.
// It has no dependencies on any other project or NuGet package — the domain
// model should be understandable and testable in complete isolation.
//
// Intentionally empty in Phase 1 (project scaffolding). Categories, metrics,
// friends, and the metric evaluation strategies arrive in Phase 2.
